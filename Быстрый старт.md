# Быстрый старт

## Что уже готово

**База данных**
- 15 моделей созданы и готовы к использованию
- Все миграции выполнены
- RBAC система настроена (роли и разрешения)

**Frontend шаблон**
- Базовый layout (`layouts/app.blade.php`)
- Главная страница (`index.blade.php`)
- Страница примеров компонентов (`examples.blade.php`)
- Готовые компоненты: Alert, Button, Card

**Стили**
- Tailwind CSS настроен
- Responsive дизайн

## Как начать работу

### 1. Просмотрите существующие файлы

**Шаблоны:**
- `resources/views/layouts/app.blade.php` - главный layout
- `resources/views/index.blade.php` - главная страница
- `resources/views/examples.blade.php` - примеры компонентов

**Модели:**
- Смотрите `app/Models/` для всех моделей
- Читайте `MODELS_DOCUMENTATION.md` для подробной документации

### 2. Используйте компоненты

```blade
{{-- Alert --}}
<x-alert type="success">
    Операция выполнена успешно!
</x-alert>

{{-- Button --}}
<x-button variant="primary">
    Нажми меня
</x-button>

{{-- Card --}}
<x-card title="Заголовок">
    Содержимое карточки
</x-card>
```

### 3. Создайте новую страницу

```bash
# Создайте файл в resources/views/
touch resources/views/my-page.blade.php
```

```blade
@extends('layouts.app')

@section('title', 'Моя страница')

@section('content')
    <div class="max-w-7xl mx-auto px-4 py-12">
        <h1 class="text-3xl font-bold">Моя страница</h1>
        <!-- Ваш контент -->
    </div>
@endsection
```

Добавьте маршрут в `routes/web.php`:
```php
Route::get('/my-page', function () {
    return view('my-page');
});
```

### 4. Работа с данными

```php
// В контроллере или маршруте
use App\Models\News;

$news = News::all(); // Все новости
$publishedNews = News::published()->get(); // Опубликованные
```

Передайте данные в view:
```php
Route::get('/news', function () {
    $news = News::published()->latest()->get();
    return view('news.index', compact('news'));
});
```

В Blade:
```blade
@foreach($news as $item)
    <x-card title="{{ $item->title }}">
        {{ $item->content }}
    </x-card>
@endforeach
```

## Полезные ссылки по проекту

- **Главная:** `http://localhost/`
- **Примеры компонентов:** `http://localhost/examples`

## Документация

📖 **DEVELOPER_GUIDE.md** - Полное руководство разработчика
📖 **MODELS_DOCUMENTATION.md** - Документация по моделям

## Структура проекта

```
website-for-young-mathematicians/
├── app/
│   ├── Models/              # Модели базы данных
│   └── Http/Controllers/    # Контроллеры (создавайте здесь)
├── resources/
│   ├── views/
│   │   ├── layouts/         # Layout шаблоны
│   │   ├── components/      # Компоненты
│   │   └── *.blade.php      # Страницы
│   └── css/app.css          # Стили
├── routes/
│   └── web.php              # Маршруты
└── database/
    └── migrations/          # Миграции БД
```

## Что нужно сделать дальше

### Приоритет 1: Аутентификация
1. Установите Laravel Breeze:
```bash
composer require laravel/breeze --dev
php artisan breeze:install blade
npm install && npm run dev
php artisan migrate
```

2. Настройте регистрацию с выбором роли

### Приоритет 2: CRUD для новостей
1. Создайте контроллер:
```bash
php artisan make:controller NewsController --resource
```

2. Создайте views:
- `resources/views/news/index.blade.php` (список)
- `resources/views/news/create.blade.php` (создание)
- `resources/views/news/edit.blade.php` (редактирование)
- `resources/views/news/show.blade.php` (просмотр)

3. Добавьте маршруты в `routes/web.php`:
```php
Route::resource('news', NewsController::class);
```

### Приоритет 3: Dashboard
Создайте личный кабинет для каждой роли:
- Участник - просмотр своей команды и результатов
- Наставник - управление командой
- Координатор - управление муниципалитетом
- Организатор - полный доступ

## Советы

💡 Используйте готовые компоненты из `resources/views/components/`
💡 Смотрите примеры на странице `/examples`
💡 Изучите модели в `app/Models/` перед началом работы
💡 Читайте документацию в корне проекта

## Помощь

Если что-то непонятно:
1. Посмотрите `examples.blade.php` - там есть примеры всех компонентов
2. Читайте `Структура проекта.md` - подробное руководство
3. Изучите `Описание моделей.md` - описание всех моделей

Удачи!
